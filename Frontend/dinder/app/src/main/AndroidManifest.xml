<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Dinder"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dinder"
        android:usesCleartextTraffic="true">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBywc6sAasZQUNvjfBrjs_xguIF0IuHlc8" />

        <activity
            android:name=".activities.OutOfLikesActivity"
            android:exported="true" />
        <activity
            android:name=".activities.AdminHomeActivity"
            android:exported="true" />
        <activity
            android:name=".activities.RestaurantProfileActivity"
            android:exported="true" />
        <activity
            android:name=".activities.ErrorScreen"
            android:exported="false" />
        <activity
            android:name=".activities.MatchesScreen"
            android:exported="false" />
        <activity
            android:name=".activities.SocialActivity"
            android:exported="true" />
        <activity
            android:name=".activities.UserHomeActivity"
            android:exported="true" />
        <activity
            android:name=".activities.UserProfileActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>